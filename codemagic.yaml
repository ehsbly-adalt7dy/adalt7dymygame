workflows:
  ios-workflow:
    name: iOS Build
    environment:
      node: latest

    scripts:
      - npm install

      # فقط Capacitor sync
      - npx cap sync ios

    artifacts:
      - ios/App/**