workflows:
  ios-workflow:
    name: iOS Build and Publish
    environment:
      node: 20
      xcode: latest
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.admar.app
      groups:
        - ios_credentials
    integrations:
      app_store_connect: "codemgck"
    scripts:
      - name: Install dependencies
        script: npm install --legacy-peer-deps

      - name: Capacitor sync
        script: npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install --verbose
          cd ../..

      - name: Fix Bundle ID
        script: |
          sed -i '' 's/adalt7dy\.mar\.com/com.admar.app/g' ios/App/App.xcodeproj/project.pbxproj

      - name: Increment build number
        script: |
          cd ios/App
          BUILD_NUMBER=$(($(app-store-connect get-latest-testflight-build-number "6778906260") + 1))
          echo "✅ New build number: $BUILD_NUMBER"
          agvtool new-version -all $BUILD_NUMBER
          cd ../..

      - name: Set up keychain
        script: keychain initialize

      - name: Fetch signing files
        script: |
          echo "$CERTIFICATE_PRIVATE_KEY" > /tmp/private_key.pem
          app-store-connect fetch-signing-files "com.admar.app" \
            --type IOS_APP_STORE \
            --certificate-key @file:/tmp/private_key.pem \
            --create

      - name: Add certificates to keychain
        script: keychain add-certificates

      - name: Use profiles
        script: |
          cd ios/App
          xcode-project use-profiles --project App.xcodeproj
          cd ../..

      - name: Build IPA
        script: |
          cd ios/App
          xcode-project build-ipa \
            --workspace App.xcworkspace \
            --scheme App
          cd ../..
          find /Users/builder/clone -name "*.ipa" 2>/dev/null

      - name: Move IPA to artifacts
        script: |
          mkdir -p build/ios/ipa
          IPA_PATH=$(find /Users/builder/clone -name "*.ipa" | head -1)
          if [ -z "$IPA_PATH" ]; then
            echo "❌ No IPA found!"
            exit 1
          fi
          cp "$IPA_PATH" build/ios/ipa/
          echo "✅ Done"
          ls -la build/ios/ipa/

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
