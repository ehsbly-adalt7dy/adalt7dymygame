workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: 22

    scripts:
      - npm install
      - chmod +x node_modules/.bin/cap || true
      - npx cap sync ios

    artifacts:
      - build/ios/ipa/*.ipa
      - ios/App/**/*.ipa