workflows:
  ios-workflow:
    name: iOS Build

    environment:
      node: latest

    scripts:
      - npm install
      - npx cap sync ios

    artifacts:
      - ios/App/**/*.ipa
      - ios/App/**/*.app