workflows:
  ios-workflow:
    name: iOS Build
    environment:
      node: latest

    scripts:
      - npm install

      # 🔥 الحل الأساسي: تشغيل cap عبر node مباشرة
      - node node_modules/@capacitor/cli/bin/capacitor.js sync ios

    artifacts:
      - ios/App/**